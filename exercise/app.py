# Imports

import json

from flask import Flask

# 1. Initialize the Application

#   a. create the Flask app

#   b. load the "database" into a python dictionary from the JSON file


# 2. Create REST endpoints for the following methods

# /items - Retrieve all items in the database

# /items/<id> - Retrieve the item with a given ID
# This should fail if an item with the given it doesn't exist

# /items/<id> - Update the item with a given ID
# This should fail if an item with the given id does not exist

# /items/<id> - Create a new item with a given ID
# This should fail if an item with the given ID already exists

# /items/<id> - Delete an item with the given id
# This should fail if an item with the given ID doesn't exist
